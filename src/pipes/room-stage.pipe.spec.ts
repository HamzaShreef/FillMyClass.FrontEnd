import { RoomStagePipe } from './room-stage.pipe';

describe('RoomStagePipe', () => {
  it('create an instance', () => {
    const pipe = new RoomStagePipe();
    expect(pipe).toBeTruthy();
  });
});
