import { TeacherFilterState } from './teacher-filter-state';

describe('TeacherFilterState', () => {
  it('should create an instance', () => {
    expect(new TeacherFilterState()).toBeTruthy();
  });
});
